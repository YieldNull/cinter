// this is a test for all the statements

real b;

int func(int a){
	return a*a;
}

int a;
int[3] arr;

void main(){
	read(a);

	while(1==(a+9*8-arr[0])){	// `while` test
    	write(9);
    	if (a>9){ // `if-else` test			
        	write(func(a)-func(a)/func(a));
    	}else{
        	if(a<15){ // `if` test
            	write(a+9/arr[1]);
        	}
    	}
	}
	return;
}