// this is a test for all the statements

real b;

void func(int[] a){
	return a[0]+b;
}

int a;
int[3] arr;

int main(){
	read(a);

	while(1==(a+9*8-arr[0])){	// `while` test
    	write(9);
    	if (a>9){ // `if-else` test
        	write(a);
    	}else{
        	if(a<15){ // `if` test
            	write(a+9/arr[1]);
        	}
    	}
	}
}